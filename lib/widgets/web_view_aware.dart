import 'package:flutter/material.dart';

class WebViewAware extends StatelessWidget {
  final Widget child;
  const WebViewAware({super.key, required this.child});
  @override
  Widget build(BuildContext context) => child;
}

